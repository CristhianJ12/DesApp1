<?xml version="1.0" encoding="utf-8"?>
<androidx.appcompat.widget.LinearLayoutCompat xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center"
    android:layout_margin="30dp"
    android:padding="20dp"
    tools:context=".retos.Reto6">

    <!--Permite el signo del numero -->
    <EditText
        android:id="@+id/numero_input"
        android:hint="Ingresa un número"
        android:inputType="numberDecimal|numberSigned"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textAlignment="center"
        android:minHeight="48dp"
        android:layout_marginBottom="16dp"/>

    <!-- Botón para evaluar el número -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/btn_evaluar"
        android:text="Evaluar Número"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:cornerRadius="8dp"
        android:layout_marginBottom="16dp"/>

    <!-- Mostrar el resultado -->
    <TextView
        android:id="@+id/resultado_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="18sp"
        android:visibility="gone"/>

</androidx.appcompat.widget.LinearLayoutCompat>